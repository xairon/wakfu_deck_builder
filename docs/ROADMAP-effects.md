# Roadmap — Encodage des effets (à reprendre ensemble)

**Dernière mise à jour : 2026-07-03.** État : **467 / 1794 effets imprimés structurés (26,0 %)** — `auto 416 · manual 51 · uncovered 1327 · ruling 452 · keyword 256 · trait 68`. Mesure : `npm run report-coverage`.

Ce document liste **tout ce qui reste** et **comment le reprendre**. Il complète le backlog d'origine (`docs/superpowers/plans/2026-06-27-effect-encoding-backlog.md`) et la note d'architecture SOTA (`docs/superpowers/specs/2026-07-01-effect-value-expressions-sota-design.md`).

---

## 1. Principes (non négociables)

1. **« Une approximation de gameplay est PIRE qu'un effet manuel. »** On n'encode un effet que si le moteur l'exécute **fidèlement**. Sinon il reste `uncovered` → rappel manuel non bloquant à la table (le jeu est jouable).
2. **Le goulot n'est PAS le moteur** (déjà aligné SOTA : effet = donnée structurée, comme Forge/XMage). C'est le **modelage fidèle** de mécaniques hétérogènes.
3. **Motif MULTI-GAPS (mesuré cette session)** : le reliquat est bloqué par **plusieurs gaps simultanés par effet** (cadre + filtre + op-de-corps + magnitude). **Toute addition unique rend ~0 en isolation** ; un effet ne compile que quand TOUS ses gaps sont comblés. Corollaire : ne pas s'attendre à de gros bonds ; viser 1-4 effets par sous-système.
4. **Stratégie retenue : DECK-DRIVEN.** On automatise d'abord les effets des **decks réellement joués** (objectif fini et motivant), pas le % brut. Le reste vit en rappel manuel.
5. **Discipline par vague** : type-check (`npm run type-check`) + suite complète (`npx vitest run`) + idempotence (`npm run compile-effects` puis `git diff --stat public/data` vide). Bump `CACHE_KEY` (cardLoader.ts) si la forme des données change. Tout nouvel **op** doit être ajouté à `OP_TO_MECHANIC` (src/data/mechanics.ts) ET `mechanicTagSchema` (src/schema/mechanics.ts), sinon `compile-effects` throw.

---

## 2. Fondations SOTA déjà bâties (réutilisables)

- **Valeur = expression (`ValueExpr`)** — AST de magnitude évalué par `evalValue` (moteur), modèle Forge `Count$`. Nœuds câblés : `fixed`, `count` (« nombre de X que vous contrôlez »). À venir : `boundCount` (replier le legacy `fromCount`), `statOf` (Force/Niveau de cible), `mirror`, `plus`.
- **Effets de remplacement (prévention de Dommages)** au choke point unique `reduceDamage` : `damagePreventionAura` (réduction entrante continue), `damageUnpreventable` (« ne peuvent pas être réduits »).
- **Coûts payés** : `costTapControlled`, `costDestroyControlled`, `costRecycle{max}`, `costRecycleControlled`, `costDiscard{max}`, **`costTapResource`** (nouveau — « payer une Ressource »), `sacrificeSelf`, `banishSelf(FromDiscard)`. Machinerie de pick interactif + binding `upTo → boundCount` (magnitude du corps via `fromCount`).
- **Ressources (modèle SANS POOL, W45)** : la Ressource n'est PAS une entité stockée — elle est DÉRIVÉE de `resourceProducers` (rulebook 4261 : toute carte contrôlée dressée, Monde/Havre-Sac sauf Protecteur, produit en s'inclinant). « payer pour <corps> » → op `costTapResource` (incliner un producteur, dédupliqué par instanceId, filtre d'Élément latent) + `<corps>`. Réutilise la machinerie `isCostTargetingOp` (skippable = décliner, abandon si aucun producteur). Câblé : « vous pouvez payer pour piocher une carte » (Smare, Kikoolarc, Champ Champ, Marlène Frimeur). Reste : Tofu Céleste (`costTapResource` + `putInPlay from:main`).
- **Production de Ressource COLORÉE (W46)** : un producteur coloré (« Produisez une Ressource [X] », un seul Élément) produit CET Élément, pas son Élément imprimé (les Pious sont Neutre en Niveau/Force mais fixent Eau/Air/Feu/Terre). Champ dérivé `card.producesElement` (compileEffects.`promoteResourceProducerTrait`, mainType Allié + mono-Élément → `coverage:"trait"`), lu par la fonction `resourceElement` (cardAttrs) que `resourceProducers`/`planCost` utilisent — DISTINCT de `producedElement` (INCHANGÉ : Dommages de combat 410.1 + filtres de pile = Élément imprimé). `manualEffects` exclut `coverage:"trait"` (métier/classe/producteur = déjà modélisé → 0 rappel). Restent MANUELS (pas d'approximation) : producteurs BI-Élément (Otomaï = choix), Zone/Équipement producteurs (statut de producteur à valider).
- **Ciblage** contrôleur-aware (`controller: "adverse"` sur damage/tap/destroy/banish/grant), filtres `sub`/`maxLevel`/`exactLevel`/`orientation`/`combatRole`/`equipType`/`requiresAttachment`/**`noEquipment`**.
- **Déclencheurs** : `onArrive`/`onOtherAppears` (watchers), `onSelfDestroyed`, `onSelfAttacks`, `onTurnStart`, **`onDamageToBearer`** (W48 : RIPOSTE DE PORTEUR — bus `bearerFrames` activé ; op `damageRiposteSource` frappe la créature qui a blessé le Porteur, cible pré-liée via `frame.riposteTargetId` ; garde anti-boucle = seul un Allié/Héros déclenche, pas l'équipement ; combat + pouvoir), statiques (`forceAura`/`keywordAura`/`costAura`/`combatDamageReduction`/`bearerBonus`). Bus `collectTriggeredEffects`.
- **Divers** : `createToken`, `chooseOne`, `conditional` (+ flag **`optional`** W47 : « si <cond>, vous POUVEZ <corps> » — la condition garde une offre effectChoices Oui/Non ; réutilisable pour les autres « au début du tour, si … vous pouvez … » dont le corps compile), mass ops (`tap/untap/damage/destroyAll`), `damageMultiTarget`, player-choice (`playerDraw/LoseStat/GainStat`), `promoteKeywords`, jetons de tour (`isTurnToken`).

---

## 3. Deck-driven — worklist des 4 starters Incarnam (15 restants)

Cible en cours : `incarnam-feca / -cra / -iop / -xelor` (ids dans `src/data/officialDecks.ts`). Croisement decks × data : script jetable `/tmp/starter_triage.mjs` (lit officialDecks.ts × public/data/incarnam.json → uncovered restants ; recréé en W47). **48 → 19** (W37 noEquipment+tofu-mutant, W38 draw fromCount, W39 costDiscard, W40 oncePerTurn+tapsSource, W41 tap/untap-multi, W42 drawTargetXp, W43 team-combat-reduction/glyphe-revigorant, W44 each-player-optional/coffre+djakky, W45 costTapResource/smare + 3 free riders, W46 production colorée/4 Pious + 4 free riders, W47 conditional{optional}/Dollarawan, W48 riposte de Porteur/Cape+Anneau du Prespic + 2 free riders, W49 actor-bind même-cible/Jeunesse d'Ogrest + Furie free rider + FIX zones Héros, W50 damageAll noXp + sujet-en-tête + élément explicite/Flèche Blizzard, W51 onDamageToSelf/Wa Wabbit, W52 designs parallélisés : projection combat local [FIX préexistant] + Exclusion removeFromCombatTarget + Dora requiresBearerInCombat + Guy Yomtella verbatim [−1 faux-auto : le compilé sous-facturait, coût 1 Air récupéré du raw → démouvu manuel], W53 Guy Yomtella RE-COUVERT via CARD_SCRIPTS [tapsSource + costTapResource{Air} + garde coût-ressource-impayable dans activateTapPower ; le pouvoir 2 « [Air][Air] : Redressez » reste manuel — pas de voie d'activation payée sans incliner], W54 bonus de pouvoir d'Allié/Guma Bobeule [powerSourceId sur toutes les frames de pouvoir + jeton teamPowerDmgMod + discriminants isDamage/pvLoss Dommages≠perte-de-PV], W55 look-N/Bonne Affaire [effectPicking gagne candidates explicites + restAction:recycle ; STRICT n=2 reste-singulier — variantes multi-reste/X manuelles], W56 choix d'Élément/Tirlangue verso + Temple Féca [chooseOne N-AIRE (effectChoiceSelect) + flag explicitElement fermant le latent W50 + tapPowers FACE-AWARE (pouvoirs du verso Héros) + icône Résistance 1 récupérée du raw], W57 actor-bind conditionnel par Famille/Charge [condSpec `selfIsFamily` + DSL « <tête>. S'il s'agit d'un [Famille], il gagne <Kw> en plus » → conditional{selfIsFamily → grantKeywordSelf} sur la cible liée ; icônes Géant/Métier récupérées du raw ; Amar Casto « gagne le Métier de votre choix » reste manuel — octroi de profession non modélisé], W58 coût de mill/Crapaud Mufle [op `costMillTop{n}` = « Défaussez la première carte de votre Pioche : … » mill déterministe du sommet vers la Défausse, ≠ costDiscard (main au choix) ; + oncePerTurn (W40) + buffForceSelf ; garde impayable dans activateTapPower], W59 récup d'icônes droppées/données seulement [Katsou Mee « [Terre] : … Si vous dépensez plus de [Terre][Terre][Terre] … détruisez » + Flèche d'Immolation « Réaction. … 2 Dommages [Feu] à … qui vient de s'incliner » — descriptions corrigées verbatim, restent manuelles : coût-Ressource-répétable-à-seuil + récence/réaction non bâtis]). Restants, groupés par **sous-système à bâtir** :

| Sous-système à bâtir                              | Effets starters concernés                                                                                            | Notes                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~Verrou once-per-turn~~ **FAIT (W40)**           | ~~bwork-mage~~ ✅ ~~crapaud-mufle~~ ✅ (W58)                                                                         | crapaud-mufle **FAIT (W58)** : coût de mill `costMillTop` (défausse du sommet de la Pioche) + once-per-turn (W40) + buffForceSelf. Flags `oncePerTurn`/`tapsSource` + jeton powerUses0 câblés dans activateTapPower.                                                                                                                                                                                                    |
| ~~Ressources — production~~ **FAIT (W45+W46)**    | ~~piou-rouge/-jaune/-vert/-bleu~~ ✅ ; reste fecaline (condition)                                                    | Coût `costTapResource` **FAIT** (W45, smare). Les 4 Pious **FAITS** (W46) : production COLORÉE via `card.producesElement`+`resourceElement` (color-fixing fonctionnel) + `coverage:"trait"` → 0 rappel. Le log-op `produceResource` a été ÉCARTÉ (footgun : dans le modèle sans-pool il gaspillerait le Piou). Reste fecaline (condition d'activation).                                                                 |
| **Magnitude dynamique** (X-cost / statOf)         | repos (X PV), coup-critique (Force doublée), colere-de-iop (X répartis) ; ~~prospection~~ ✅ W42 ~~merelyne~~ ✅ W60 | prospection FAIT (`drawTargetXp`). **merelyne FAIT (W60)** : op `costPayX` (coût variable X = incliner X producteurs → boundCount) + `destroyTarget{exactLevelFromCount}` (Niveau X) ; CARD_SCRIPTS coût composé [Incliner]+X, 2 éditions. Reste = repos (X-cost + play-restriction havreSac), coup-critique (statOf double-Force + restriction/tour), colere-de-iop (X + répartition libre) — `costPayX` réutilisable. |
| ~~Porteur / riposte~~ **FAIT (W48+W52)**          | ~~cape-du-prespic~~ ✅ ~~anneau-du-prespic~~ ✅ (W48) ~~dora~~ ✅ (W52)                                              | Riposte **FAITE (W48)** : bus `bearerFrames` activé + op `damageRiposteSource` (Cape/Anneau du Prespic + 2 réimpressions). Dora **FAITE (W52)** : flag `requiresBearerInCombat` (garde activateTapPower + fenêtre de réaction 706.5 + 2 retouches GameBoard).                                                                                                                                                           |
| **Récence** (« qui vient de subir / s'incliner ») | fleche-d-immolation, glyphe-incandescent, fecaline (« venez de jouer »)                                              | counters.damage est TURN-scoped, pas combat-scoped → besoin d'un marqueur de récence fidèle. wa-wabbit **FAIT (W51)** ; crapaud-mufle **FAIT (W58** mill-cost, pas récence).                                                                                                                                                                                                                                            |
| ~~Each-player optionnel~~ **FAIT (W44)**          | ~~coffre-malveillant~~ ✅ ~~djakky-chwan~~ ✅ ~~smare~~ ✅ (W45, « payer pour »)                                     | « chaque joueur PEUT … » (par-joueur optionnel). smare = `costTapResource` (W45).                                                                                                                                                                                                                                                                                                                                       |
| **Placement en combat** (½ FAIT W52)              | ~~exclusion~~ ✅ (W52) ; reste bond                                                                                  | Exclusion **FAITE (W52)** : op `removeFromCombatTarget` + PROJECTION du combat local dans rulesCtx (fix préexistant : les filtres combatRole étaient sans cible en partie locale). Reste bond (« Placez … en bloqueur devant … » = déclaration de blocage forcée).                                                                                                                                                      |
| **Annulation / File** (réaction)                  | echec-critique (« Annulez … qui vient d'être joué »)                                                                 | Interaction avec la file d'attente.                                                                                                                                                                                                                                                                                                                                                                                     |
| ~~Look-N pile~~ **FAIT (W55)**                    | ~~bonne-affaire~~ ✅                                                                                                 | Op `lookTopPick` (candidats explicites du pick + restAction recycle). Variantes multi-reste (anneau-fioutioure, jock-et-lack) et X (faille-temporelle) restent manuelles.                                                                                                                                                                                                                                               |
| ~~Actor-bind même-cible~~ **FAIT (W49)**          | ~~jeunesse-d-ogrest~~ ✅ (« Redressez X. Il gagne +2 ») ; + furie free rider                                         | `actor:"target"` : la cible choisie par l'op de ciblage (tête) devient le sujet du corps « Il/Elle … » (le moteur réécrit sourceId). Jumeau de `costTarget`. A aussi corrigé le bug de zones « ou Héros » (Havre-Sac inclus).                                                                                                                                                                                           |
| ~~Double coût~~ **FAIT (W40)**                    | ~~amulette-akwadala~~ ✅                                                                                             | Flag `tapsSource` (requiresIncline + paidOps → l'activation incline aussi la source). Garde : coût de défausse imposé impayable refusé AVANT de consommer inclinaison/verrou.                                                                                                                                                                                                                                           |
| ~~Tap/untap-multi (fromCount)~~ **FAIT (W41)**    | ~~choc-temporel~~ ✅ + ~~parchemin-d-agilite~~ ✅                                                                    | Ops `tapMultiTarget`/`untapMultiTarget` (ciblage répété borné = boundCount, cibles distinctes ; jumeaux de damageMultiTarget mais SET_ORIENTATION). UN sous-système = 2 starters.                                                                                                                                                                                                                                       |
| **Icône perdue au scrape**                        | charge (« il gagne [.] en plus »)                                                                                    | Re-scrape source (wtcg-return.fr) avant d'encoder.                                                                                                                                                                                                                                                                                                                                                                      |

> **ROI le plus propre à reprendre** : (a) **Ressources — production** (`produceResource` no-op) : lève les 4 Pious d'un coup, purement additif, zéro régression (fondation `costTapResource` déjà là) ; (b) **Tofu Céleste** (`costTapResource` + `putInPlay from:main` — les deux ops existent ; seul point à vérifier : `putInPlay{sub:"Tofu"}` doit viser LE Tofu Céleste nommé, pas n'importe quel Tofu de la main) ; (c) **magnitude dynamique** `statOf`/valeur-X (transverse : repos, merelyne, coup-critique…) — étend `ValueExpr` déjà bâti.

Après les 4 starters Incarnam : passer aux **starters Bonta-Brâkmar** (`bonta-brakmar-sadida/-sram`) puis aux decks Dofus Mag joués.

---

### Dettes de fidélité PRÉEXISTANTES relevées par les revues (à traiter en vagues dédiées)

- **pvLoss traverse la Résistance** : « le Héros de votre choix perd N PV » (damageTarget{pvLoss}) route via resolveDamageTarget/reduceDamage — une perte de PV n'est PAS un Dommage (410.3), la Résistance ne devrait pas s'appliquer. W54 a posé le discriminant (le bonus ne s'y applique plus) sans corriger le routage.
- **damageOppHero{isDamage} court-circuite reduceDamage** : « inflige N Dommages au Héros adverse » (adjustCounter direct) — pas de Résistance du Héros adverse, pas d'événement damageDealt (les déclenchés ne voient pas ces Dommages). 0 occurrence compilée aujourd'hui ; router la variante isDamage via resolveDamageTarget changerait le comportement de cartes couvertes → vague dédiée.

---

## 4. Backlog global (hors starters) — par fréquence mesurée

Incidence dans les ~1373 `uncovered` (chevauchante) — priorise les sous-systèmes transverses :

| Cluster                                           | ~Effets                                             | Type                                                           |
| ------------------------------------------------- | --------------------------------------------------- | -------------------------------------------------------------- |
| Déclencheurs sujet ≠ soi (« Quand un autre X … ») | 331 (dont 151 = « self apparaît » à corps manquant) | bus veilleur (onOther\*) — le corps est souvent déjà encodable |
| Continu « Tant que … » (au-delà des auras faites) | ~145                                                | modèle continu « layers » étendu                               |
| Porteur / Équipement dynamique                    | ~112                                                | modèle Porteur (riposte, bonus dynamique)                      |
| Magnitude dynamique (« égal à … »)                | ~100                                                | étendre `ValueExpr`                                            |
| Restriction « ne peut pas … » ciblée              | ~81                                                 | restriction continue                                           |
| Aléatoire / Chi-Fu-Mi                             | ~40                                                 | RNG rejouable (seed)                                           |
| Ressource                                         | ~38                                                 | pool de Ressources                                             |
| Réaction / Annuler                                | ~37                                                 | file d'attente                                                 |
| Data-bloqués (icônes perdues)                     | variable                                            | **re-scrape** wtcg-return.fr requis (ne jamais deviner)        |

> **Insight** : parmi les 331 triggers, **151 sont « Quand [self] apparaît »** dont le cadre est DÉJÀ reconnu — seul le **corps (op d'action)** manque. Étendre les ops d'action débloque là ET sur tous les cadres à la fois (le meilleur levier transverse structurel).

---

## 5. Accélérateur (à évaluer ensemble)

**Harnais LLM → schéma** : un LLM traduit le texte de carte → ops structurées, **sous garde-fous durs** (schéma Zod strict + toutes ops exécutables + passe de fidélité + revue humaine avant `auto`). Mesure de cette session : l'authoring par lot naïf a un **faible taux** sur le reste (goulot = vocabulaire manquant, pas débit). Donc l'apport réel du LLM = (a) **générer le backlog de mécaniques manquantes classé par fréquence** (pilote §4), (b) éponger la fine queue « phrasé raté ». **Pas** une baguette de couverture. Décider si on le construit.

---

## 6. Comment reprendre (recette)

1. `npm run report-coverage` — état actuel.
2. Choisir la cible (deck-driven : recréer le croisement decks × uncovered). Script jetable type :
   ```js
   // lit src/data/officialDecks.ts (noms de cartes) × public/data/*.json (coverage)
   // → liste des effets uncovered du/des deck(s) visé(s), avec id/index/élément.
   ```
3. Trier par sous-système (§3/§4). Prendre **un** sous-système.
4. Vague : type+Zod (schéma) → OP_TO_MECHANIC + mechanicTagSchema si nouvel op → parse DSL → handler moteur fidèle → tests (DSL + moteur isolé) → `compile-effects` → `report-coverage` → type-check + suite + idempotence.
5. Commit atomique. `CACHE_KEY` bump si forme données changée.

**Garde-fous vérifiés** : `scripts/` n'est pas type-checké par vue-tsc (vigilance manuelle). Toujours lancer la suite COMPLÈTE post-merge (a déjà attrapé des désyncs CACHE_KEY et des intrications git). Destruction d'un Allié (même le sien) donne l'XP à l'adversaire (415.1).
